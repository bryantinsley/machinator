#!/bin/bash
# Pre-commit hook - runs beads sync, formatting, and tidy commands
# Enable: git config core.hooksPath scripts/hooks

set -e

# ═══════════════════════════════════════════════════════════════════════════════
# 1. Run beads pre-commit hook (flushes changes to JSONL)
# ═══════════════════════════════════════════════════════════════════════════════
if command -v bd &> /dev/null; then
    bd hooks run pre-commit "$@" || true
fi

# ═══════════════════════════════════════════════════════════════════════════════
# 2. Note: bazel mod tidy is NOT run automatically - it can remove needed deps
#    Run manually when you update go.mod: bazel mod tidy
# ═══════════════════════════════════════════════════════════════════════════════


# ═══════════════════════════════════════════════════════════════════════════════
# 3. Go mod tidy (updates go.mod and go.sum)
# ═══════════════════════════════════════════════════════════════════════════════
if [ -f "go.mod" ] && command -v go &> /dev/null; then
    echo "🔧 Running go mod tidy..."
    go mod tidy 2>/dev/null || true
    git add go.mod go.sum 2>/dev/null || true
fi

# ═══════════════════════════════════════════════════════════════════════════════
# 4. Format Bazel files with buildifier
# ═══════════════════════════════════════════════════════════════════════════════
BAZEL_FILES=$(git diff --cached --name-only --diff-filter=ACM | grep -E '(BUILD|WORKSPACE|\.bzl)$' || true)

if [ -n "$BAZEL_FILES" ]; then
    if command -v buildifier &> /dev/null; then
        echo "🔧 Formatting Bazel files..."
        for file in $BAZEL_FILES; do
            if [ -f "$file" ]; then
                buildifier -mode=fix "$file"
                git add "$file"
            fi
        done
        echo "✅ Bazel files formatted"
    else
        echo "⚠️  buildifier not found. Install: brew install buildifier"
    fi
fi

# ═══════════════════════════════════════════════════════════════════════════════
# 5. Format Go files with gofmt
# ═══════════════════════════════════════════════════════════════════════════════
GO_FILES=$(git diff --cached --name-only --diff-filter=ACM | grep '\.go$' || true)

if [ -n "$GO_FILES" ]; then
    if command -v gofmt &> /dev/null; then
        echo "🔧 Formatting Go files..."
        for file in $GO_FILES; do
            if [ -f "$file" ]; then
                gofmt -w "$file"
                git add "$file"
            fi
        done
        echo "✅ Go files formatted"
    fi
fi

echo "✅ Pre-commit checks complete"
