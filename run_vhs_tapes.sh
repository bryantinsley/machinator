#!/bin/bash
# VHS generation has been moved to GitHub Actions to avoid macOS sandbox issues and improve performance.
# This script is now a placeholder.

echo "VHS generation is now handled by CI."
echo "1. Push your changes."
echo "2. The 'VHS' GitHub Action will run automatically."
echo "3. It will commit the generated GIFs back to the repository."
echo ""
echo "To wait for the result, run:"
echo "  ./scripts/wait_for_vhs_ci.sh"